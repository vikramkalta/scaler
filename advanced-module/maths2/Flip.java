import java.util.ArrayList;

public class Flip {
    public static void main(String args[]) {
        // int[] arr = {3,4,-1,1};
        // for (int i = 0; i < arr.length; i++) A.add(arr[i]);
        System.out.println(solve("01010001"));
    }

    public static ArrayList<Integer> solve(String A) {
        int len = A.length();
        return new ArrayList<>();
    }
}
