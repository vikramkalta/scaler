#include <iostream>
#include <string>
#include <vector>

using namespace std;

string getVowels(string A) {
    int len = A.size();
    // string
    for (int i = 0; i < len; i++) {

    }
}

int main() {
    return 0;
}